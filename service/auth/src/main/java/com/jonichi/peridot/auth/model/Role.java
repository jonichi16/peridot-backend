package com.jonichi.peridot.auth.model;

/**
 * Enum representing the roles a user can have in the system.
 *
 * <p>The {@code Role} enum defines the available roles for users. Currently, the only defined
 * role is {@code USER}, which represents a standard user with access to basic application
 * functionalities.</p>
 */
public enum Role {
    USER
}
